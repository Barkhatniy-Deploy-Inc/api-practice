# Зебра-стат: Регламент развертывания и эксплуатации (DOD)

Этот документ предназначен для DevOps-инженера и содержит все технические детали для запуска Зебра-стат API в продакшн-среде.

---

## 1. Стек технологий
*   **Язык:** Python 3.10+
*   **Фреймворк:** FastAPI (ASGI)
*   **База данных:** SQLite (локальный файл `dtp.db`)
*   **Контейнеризация:** Docker + Docker Compose

---

## 2. Переменные окружения
Система требует настройки следующих параметров:

| Переменная | Описание | Обязательно |
|------------|----------|-------------|
| `X_API_KEY` | Мастер-ключ (мин. 32 символа) для генерации студенческих токенов. | Да |
| `DATABASE_URL` | URL базы данных (по умолчанию `sqlite+aiosqlite:///./dtp.db`). | Нет |
| `DEBUG_MODE` | Включение подробных логов (true/false). | Нет |

---

## 3. Быстрый запуск (Docker)
Для развертывания одной командой выполните:
```bash
# Укажите ваш секретный ключ в команде или в .env файле
X_API_KEY="ваш_секретный_ключ_длиной_32_символа" docker-compose up -d --build
```

---

## 4. Сбор логов и мониторинг
*   **Логи контейнера:** выводятся в `stdout` (рекомендуется настроить драйвер `json-file` или `gelf`).
*   **Файловые логи:** монтируются в локальную папку `./logs` из контейнера.
*   **Health Check:** доступен по эндпоинту `GET /health`. Возвращает `{"status":"ok"}`.

---

## 5. Обслуживание базы данных
Поскольку используется SQLite, для резервного копирования достаточно делать snapshot файла `dtp.db`. При миграции на PostgreSQL потребуется обновление `app/core/config.py`.

---

## 6. Автоматизация (CI/CD)
В репозитории настроен GitHub Actions (`.github/workflows/ci.yml`). При каждом пуше в ветки `main/master` запускается полный цикл тестов (56 тестов) с замером покрытия.

---

## 7. Управление секретами
Все чувствительные данные должны передаваться через переменные окружения. **Никогда не храните их в коде или Dockerfile.**

### Настройка в CI/CD (GitHub Secrets):
Для корректной работы автоматических тестов и деплоя добавьте в настройки репозитория:
*   `X_API_KEY`: мастер-ключ (см. `.env.example`).
*   `AUTH_SALT`: соль для хеширования.

### Безопасность БД:
*   Файл `dtp.db` содержит персональные данные и хеши ключей. 
*   Доступ к папке `/app/data` на хосте должен быть ограничен (права 600 или 700).

---

## 8. Инициализация (First Run)
Перед первым запуском API необходимо подготовить базу данных:
1.  Убедитесь, что `DATABASE_URL` настроен.
2.  Выполните: `python scripts/init_db.py` (создание таблиц).
3.  Выполните: `python scripts/seed_db.py` (наполнение справочников).
